var toc_content = `1.1.4 K-means clustering (Clustering) 
K-means is a popular unsupervised type of ML algorithm used for clustering data 
points into groups based on similarity. Clustering is considered an important concept 
to help find a structure or a pattern in a set of unknown data. Clustering algorithms 
such as K-means are meant to process data and discover clusters (data points that 
can be grouped) when they are present in a data set. Such clusters can be used to 
extract useful information and to potentially assist in identifying intrusions, 
cyberattacks and malware26. 
A known limitation of K-means is that it assumes that all clusters have equal sizes and 
variances27. Another limitation is that the algorithm is limited to linear boundaries of 
data.  

`;