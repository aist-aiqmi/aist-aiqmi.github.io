var toc_content = `1.2.2 Convolutional Neural Networks (CNNs) 
CNNs are types of Neural Networks that are specifically designed for image 
processing tasks, such as object recognition and classification. CNNs adopt Deep 
Learning (DL)-based approaches that can efficiently model very large data sets. CNNs 
use a series of convolutional and pooling layers to extract increasingly abstract 
features from input images. The convolutional layers apply filters to the input image to 
identify patterns and features, while the pooling layers down sample the feature maps 
to reduce the computational complexity of the network. The output of the final layer of 
the CNN is then fed into a fully connected layer that performs the classification task. 
Their success followed the huge breakthrough in GPUs with significant data 
processing capacity. However, they can be computationally intensive as they require 
graphical processing units (GPUs) to train the models. 
In cybersecurity, CNNs have been used for intrusion detection tasks38. 

`;