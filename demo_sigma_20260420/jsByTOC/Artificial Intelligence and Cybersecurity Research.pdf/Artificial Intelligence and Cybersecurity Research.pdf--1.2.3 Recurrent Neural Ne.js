var toc_content = `1.2.3 Recurrent Neural Networks (RNNs) 
RNNs are a type of neural network that is particularly well-suited for sequential data, 
such as time series or text data. RNNs are designed to handle inputs of variable 
length, by processing one element at a time while also maintaining an internal state 
that summarises the previous inputs. This internal state is passed from one time step 
to the next, allowing the network to capture dependencies and patterns that exist over 
time. 
RNNs are typically used for intrusion detection in the KDD99 data sets (see section 
2.4) with high-levels of accuracy39. 

`;